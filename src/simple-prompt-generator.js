/**
 * This function is a stand-in for a future object that will pull prompts from a
 * JSON file.
 */
// TODO: delete this and replace the instantiation of `.get` with the new `prompts.getRandom`
module.exports = {
  get: () => "Draw a philanthropic fox."
};
